// @ts-check
import { defineConfig } from "astro/config";

// Enable Tailwind via the official integration if desired later.
// For now using raw PostCSS pipeline; can add: import tailwind from '@astrojs/tailwind';

// https://astro.build/config
export default defineConfig({
  // integrations: [tailwind()],
});
